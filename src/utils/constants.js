import Login from "@/components/templates/login-register/Login";

export const authTypes = {
    LOGIN: "login",
    REGISTER: "register",
};

export const roles = {
    USER: "USER",
    ADMIN: "ADMIN",
}